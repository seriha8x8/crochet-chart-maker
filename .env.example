# Optional: enable Supabase cloud sync (sign-in + save/load your chart).
# Without these, the app still works fully using browser localStorage.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
